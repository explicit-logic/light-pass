function HeaderLocale({ children }: { children: React.ReactNode }) {
  return <span className="text-white font-semibold bg-gray-900 rounded-lg px-2 py-1">{children}</span>;
}

export default HeaderLocale;
